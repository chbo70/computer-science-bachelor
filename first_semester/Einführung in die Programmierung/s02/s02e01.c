#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int value = -56; // TODO: Choose a value
    if (value < 0) {
        printf("negative\n");
    }
    else {
        printf("positive\n");
    }
    return EXIT_SUCCESS;
}